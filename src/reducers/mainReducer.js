

const _defaultState = {
  gifURL: null
};

const MainReducer = (state = _defaultState, action) => {
  let newState = {};

  switch (action.type) {
    default:
      return state;
  }

};

export default MainReducer;
